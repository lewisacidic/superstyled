import { style } from './style'

export const cursor = style({
  prop: '$cursor',
  css: 'cursor'
})

export const userSelect = style({
  prop: '$userSelect',
  css: 'userSelect'
})

export default [cursor, userSelect]
